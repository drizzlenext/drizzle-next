import { db } from "@/lib/db";
import { DrizzleAdminConfig } from "drizzle-admin/types";
import { {{userObj.pluralCamelCase}}Table } from "./_tables/users-table";

export const config: DrizzleAdminConfig = {
  basePath: "/admin",
  schema: {
    {{userObj.pluralCamelCase}}: {{userObj.pluralCamelCase}}Table,
  },
  db: db,
  dbDialect: "{{dbDialect}}",
};
