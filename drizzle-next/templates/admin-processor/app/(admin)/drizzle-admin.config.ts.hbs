import { db } from "@/lib/db";
import { DrizzleAdminConfig } from "drizzle-admin/types";
import { {{userObj.pluralCamelCase}}TableConfig } from "./_lib/{{userObj.pluralCamelCase}}-table.config";

export const config: DrizzleAdminConfig = {
  basePath: "/admin",
  schema: {
    {{userObj.pluralCamelCase}}: {{userObj.pluralCamelCase}}TableConfig,
  },
  db: db,
  dbDialect: "{{dbDialect}}",
};
