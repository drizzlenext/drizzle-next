"use client";

import {
  Table2Icon,
  LayoutDashboardIcon,
  UserIcon,
  LogOutIcon,
} from "lucide-react";
import Link from "next/link";
import { cn } from "@/lib/utils";
import { usePathname } from "next/navigation";

const items = [
  { title: "Dashboard", url: "/dashboard", icon: LayoutDashboardIcon },
  // [CODE_MARK private-sidebar-items]
  { title: "Profile", url: "/profile", icon: UserIcon },
  { title: "Sign out", url: "/signout", icon: LogOutIcon },
];

export function PrivateSidebar() {
  const pathname = usePathname();

  return (
    <div className="fixed top-12 z-20 flex min-h-screen w-10 flex-col border-r border-primary-300 bg-primary-200 text-sm dark:border-primary-700 dark:bg-primary-900 sm:w-40">
      {items.map((item) => (
        <Link
          key={item.title}
          href={item.url}
          className={cn(
            "flex items-center gap-2 p-2 hover:bg-primary-300 dark:hover:bg-primary-700",
            pathname === item.url && "bg-primary-300 dark:bg-primary-700",
          )}
        >
          <item.icon className="h-4 w-4 flex-shrink-0" />
          <span className="hidden sm:block">{item.title}</span>
        </Link>
      ))}
    </div>
  );
}