import { User } from "@/schema/users";
import Link from "next/link";
import { Avatar } from "../ui/avatar";
import { User2Icon } from "lucide-react";

export function PrivateHeader({ user }: { user: User }) {
  return (
    <header className="flex h-8 shrink-0 items-center gap-2 bg-zinc-200 dark:bg-zinc-900 border-b border-zinc-300 dark:border-zinc-700 fixed w-full z-20 justify-between">
      <div className="flex w-full justify-between items-center">
        <div className="font-mono font-bold px-2">
          <Link href="/">drizzle-next</Link>
        </div>
        <div className="flex gap-2 px-2">
          <Avatar src={user.image}>
            <User2Icon />
          </Avatar>
          <div>{user.name}</div>
        </div>
      </div>
    </header>
  );
}
