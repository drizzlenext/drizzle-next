import { User } from "@/schema/{{userObj.pluralKebabCase}}";
import Link from "next/link";
import { Avatar } from "../ui/avatar";
import { User2Icon } from "lucide-react";

export function PrivateHeader({ user }: { user: User }) {
  return (
    <header className="fixed z-20 flex h-12 w-full shrink-0 items-center justify-between gap-2 border-b border-primary-300 bg-primary-50 dark:border-primary-700 dark:bg-primary-900">
      <div className="flex w-full items-center justify-between">
        <div className="px-2 font-mono font-bold">
          <Link href="/">drizzle-next</Link>
        </div>
        <div className="flex gap-2 px-2">
          <Avatar src={user.image}>
            <User2Icon />
          </Avatar>
          <div>{user.name}</div>
        </div>
      </div>
    </header>
  );
}
