import * as React from "react";
import { cn } from "@/lib/utils";

const Button = React.forwardRef<
  HTMLButtonElement,
  React.ButtonHTMLAttributes<HTMLButtonElement>
>(({ className, ...props }, ref) => {
  return (
    <button
      className={cn(
        "bg-primary-950 px-4 py-2 text-primary-50 hover:bg-primary-950/90 disabled:bg-primary-950/50 dark:bg-primary-50 dark:text-primary-950 dark:hover:bg-primary-50/90 dark:disabled:bg-primary-50/50",
        className,
      )}
      ref={ref}
      {...props}
    />
  );
});
Button.displayName = "Button";

export { Button };
