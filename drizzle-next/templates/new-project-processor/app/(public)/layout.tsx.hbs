import { PublicShell } from "@/components/public/layout/public-shell";
import { PublicHeader } from "@/components/public/layout/public-header";
import { PublicContent } from "@/components/public/layout/public-content";
import { PublicFooter } from "@/components/public/layout/public-footer";

export default function Layout({ children }: { children: React.ReactNode }) {
  return (
    <PublicShell>
      <PublicHeader />
      <PublicContent>{children}</PublicContent>
      <PublicFooter />
    </PublicShell>
  );
}

export const dynamic = "force-dynamic";
