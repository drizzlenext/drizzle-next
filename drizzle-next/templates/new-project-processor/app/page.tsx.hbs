import { Button } from "@/components/ui/button";
import { DarkModeToggle } from "@/components/ui/dark-mode";
import {
  CloudDrizzleIcon,
  ExternalLinkIcon,
  GaugeIcon,
  LayoutDashboardIcon,
} from "lucide-react";
import Link from "next/link";

export default function Page() {
  return (
    <div className="flex h-screen flex-col">
      <div className="flex flex-1 flex-col justify-center">
        <main className="m-auto flex w-72 flex-1 flex-col justify-center gap-5">
          <div className="ml-auto">
            <DarkModeToggle />
          </div>
          <h1 className="text-primary-600 dark:text-primary-400 flex items-center gap-2 font-mono text-4xl">
            <CloudDrizzleIcon size={36} /> Drizzle Next
          </h1>
          <Link href="https://www.drizzle-next.com" target="_blank">
            <Button className="flex w-full items-center justify-center gap-2">
              <ExternalLinkIcon /> View Documentation
            </Button>
          </Link>
          <Link href="/dashboard">
            <Button className="flex w-full items-center justify-center gap-2">
              <LayoutDashboardIcon /> User Dashboard
            </Button>
          </Link>
          <Link href="/admin">
            <Button className="flex w-full items-center justify-center gap-2">
              <GaugeIcon /> Admin Dashboard
            </Button>
          </Link>
        </main>
      </div>
    </div>
  );
}
