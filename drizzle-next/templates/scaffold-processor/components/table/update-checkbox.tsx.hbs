      <FormControl>
        <Label htmlFor="{{validatedColumn.caseVariants.originalCamelCase}}">{{validatedColumn.caseVariants.originalCapitalCase}}</Label>
        <Checkbox name="{{validatedColumn.caseVariants.originalCamelCase}}" id="{{validatedColumn.caseVariants.originalCamelCase}}" defaultChecked={  {{tableObj.singularCamelCase}}.{{validatedColumn.caseVariants.originalCamelCase}} ?? false } />
        {state.errors?.{{validatedColumn.caseVariants.originalCamelCase}}?.map((error) => (
          <FormMessage key={error}>{error}</FormMessage>
        ))}
      </FormControl>