import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    serverActions: {
      allowedOrigins: ["demo.drizzle-next.com"],
      bodySizeLimit: "5mb",
    },
  },
};

export default nextConfig;
